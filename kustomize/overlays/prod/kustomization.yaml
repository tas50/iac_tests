apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

replicas:
  - name: kustomize-web
    count: 3

patches:
  - path: patch-resources.yaml
    target:
      kind: Deployment
      name: kustomize-web
